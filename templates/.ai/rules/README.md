# Rules — Behavioral Constraints

Add rules here. Example: `edit-detail-consistency.md`, `agent-learning-from-errors.md`.

**Format:** Stubs in tool dirs (`.cursor/rules/`, `.claude/`) point to canonical `.ai/rules/` files.
