# Session Close — Lite Compound Path

**When:** User wants to close session with minimal steps (no full compound).

**Steps:** Step 0 (pre-compact dump) + Step 3b (thread-archive) + Step 3c (open-items) + Step 10 (report).

Skip: topic writes, BACKLOG, design system, memory index.

**Cross-ref:** Full compound in `compound.md`.
