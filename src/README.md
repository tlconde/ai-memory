# Source — Package Source

Placeholder for future implementation.

**Planned:**
- `cli.ts` — ai-os CLI (init, sync, audit, start, prune)
- `mcp-server.ts` — MCP Memory Server (get_memory_index, search_memory, validate_context, commit_memory)
- `schema/` — Canonical schema validators and types
